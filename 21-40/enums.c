// enum is a way to assign names to integer values
// if you don't give the first element an integer value using = it will automatically bet set to 0

enum day {sun, mon, tue, wed, thu, fri, sat};
enum coordinals {north = 0, east = 90, south = 180, west = 270};
enum months {jan = 1, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec};